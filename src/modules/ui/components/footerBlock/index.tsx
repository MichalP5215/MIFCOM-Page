/* eslint-disable */
import React from 'react';
import { InterfaceFooterBlock } from './types';

const FooterBlock: React.FunctionComponent<InterfaceFooterBlock> = (props) => {
    return (<>
        <div>Footer Block</div>
    </>);
};

export default FooterBlock;
